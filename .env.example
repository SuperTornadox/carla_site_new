DATABASE_URL="postgresql://carlasite:carlasite@localhost:5888/carlasite?schema=public"

# 32+ chars recommended
SESSION_PASSWORD="change-me-to-a-long-random-string"

ADMIN_USERNAME="admin"
ADMIN_PASSWORD="change-me"

# Used by import scripts to target the legacy WP site
WP_BASE_URL="https://carlagannis.com/blog"

# Controls how the importer handles WordPress uploads:
# - blob: upload to Vercel Blob + rewrite HTML (requires BLOB_READ_WRITE_TOKEN)
# - public: download into `public/blog/wp-content/uploads`
# - none: do not download or upload uploads (HTML keeps existing upload URLs)
MEDIA_MODE="blob"

# Required for MEDIA_MODE=blob (also enables /admin media upload)
# BLOB_READ_WRITE_TOKEN="..."

